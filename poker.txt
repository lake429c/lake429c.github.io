var arr = [];
for(var i = 0; i < 5; i++){
  var tmp_suit = Math.floor(Math.random() * 4);
  var tmp_num = Math.floor(Math.random() * 13)+1;
  arr.push({suit: tmp_suit, num: tmp_num});
};
arr.sort(function(a,b){
  if(a.num < b.num) return -1;
  if(a.num > b.num) return 1;
  return 0;
});

arr;
